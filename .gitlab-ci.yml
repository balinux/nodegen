image: docker

services:
  - docker:dind

stages:
  - test
  - deploy

test:
  stage: test
  only:
    - dev
    - master
  script:
    - echo run tests in this section

step-deploy-prod:
  stage: deploy
  only:
    - master
  script:
    - docker build -t balinux/nodegen .
    - docker run -p 9999:9999 -d balinux/nodegen
  environment: master
  when: manual
  
